# MKF 3%/4% 历史 70% 目标触达回测策略

## 唯一规范口径

凡是复现、延续或比较“MKF 3%/4% 胜率约 70%”的历史回测，必须使用本文件定义的 **target-hit-only / target-zero** 口径。除非用户在当前任务中明确要求另一种研究问题，否则不得把 timeout open/close、止损亏损、手续费、滑点、税费、仓位、真实 P&L、close-to-close 胜率或 positive return rate 混入本口径。

历史归档方法标识：

```text
study = mkf_post_cross_lag0_to_lag7_target_zero_return_grid
schema_version = ncn_mkf_post_cross_lag_target_grid_v3
```

核心归档文件：

```text
docs/research/results/mkf/lag0-to_lag5_target_grid/mkf-lag0-to-lag5_target-grid-20260827-143915.json
docs/research/results/mkf/lag0-to_lag5_target_grid/mkf-lag0-to-lag5_target-grid-20260827-143915.csv
```

## 信号、lag 与入场

1. 父信号：`mkf_red_blue_cross20_green_exit_under80_mask`。
2. lag 分组：从父信号所在股票可交易行开始，按 `lag0..lag7` 生成独立事件；停牌或非交易行不消耗 lag。
3. 硬门槛：`production_gate_mask` 应用于每个 lag 信号行。
4. 入场价：lag 信号收盘确认后的下一股票可交易日开盘价 `entry_open`。
5. 重复入场：同一股票、同一 lag、同一入场行只保留一次，避免重复计算同一个入场事件。

## 目标触达定义

对每个组合 `(lag, horizon, target_pct)`：

```text
target_price = entry_open * (1 + target_pct / 100)
target_hit = any(high from T+1 through T+N) >= target_price
```

其中：

- `T` 是入场日。
- `T+1..T+N` 是入场后可卖出的股票可交易日窗口。
- 买入日最高价不计入 `target_hit`，因为 A 股新买仓位不能当日卖出。
- 只要窗口内任一可卖日最高价触达目标价，就记为目标触达。
- mature 样本必须有完整的 `T+1..T+N` high 数据；partial 样本可用于较短 horizon，但不能用于缺失未来 high 的 horizon。

## 胜率与排序字段

本口径中的“胜率”只表示目标触达率：

```text
target_hit_rate = target_hits / n
```

历史排序字段是 target-zero 简化收益：

```text
mean_target_zero_return = target_pct / 100 * target_hit_rate
```

等价的单样本简化口径：

```text
sample_return = target_pct / 100 if target_hit else 0
```

注意：`mean_target_zero_return` 不是实盘收益，不是真实 P&L，不是 timeout 退出收益；它只是历史归档用于网格排序的研究字段。未触达目标的样本只在这个排序字段里固定贡献 0。

## 严禁混用的口径

复现或比较本策略时，以下字段或假设不得进入标准结果、排序或结论：

- `T+N open / entry_open - 1` timeout-open 退出收益。
- `T+N close / entry_open - 1` timeout-close 退出收益。
- close-to-close `win_rate`。
- `positive_return_rate`。
- 止损触发亏损或 stop-loss P&L。
- 手续费、滑点、印花税、成交可得性、仓位、真实撮合、真实 P&L。
- 把 archived selection-run 小样本结果当成全量全市场验证。

如需研究上述任一问题，必须单独命名脚本、schema、输出文件和结论，不得复用 `ncn_mkf_post_cross_lag_target_grid_v3` 的 70% 历史胜率表述。

## 标准输出字段

标准 JSON/CSV 每个 `lag × period × horizon × target_pct` 格子必须包含：

```text
lag
period
horizon
target_pct
target_label
n
target_hits
target_hit_rate
target_hit_wilson_lower_95
target_hit_wilson_upper_95
mean_target_zero_return
entry_dates
codes
events
retention_vs_parent_crosses
```

标准输出不得包含：

```text
mean_sample_return
median_sample_return
timeout_count
timeout_mean_open_return
timeout_median_open_return
positive_return_rate
mean_realized_return
median_realized_return
realized_gross_return
```

JSON readout 的主排序 key 必须是：

```text
best_point_readout.best_by_mean_target_zero_return
```

## 已核对历史结果

### 全量旧网格：lag0..lag5，target 3%/4%，T+1..T+20

| target | 最佳 lag | horizon | n | target_hits | target_hit_rate | Wilson lower 95% | mean_target_zero_return | codes | entry_dates |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 3% | 2 | T+20 | 19,492 | 14,533 | 74.56% | 73.94% | 2.2368% | 2,475 | 1,162 |
| 4% | 5 | T+20 | 18,835 | 12,899 | 68.48% | 67.82% | 2.7394% | 2,425 | 1,158 |

结论：严格限定 lag0..lag5 时，3% 是稳定 70%+ 目标触达区域；4% 是 70% 左右但全期未超过 70%。

### 归档 JSON best_by_target_pct：lag0..lag7，target 3%/4%，T+1..T+20

| target | 最佳 lag | horizon | n | target_hits | target_hit_rate | Wilson lower 95% | mean_target_zero_return | codes | entry_dates |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 3% | 7 | T+20 | 18,250 | 13,803 | 75.63% | 75.00% | 2.2690% | 2,368 | 1,159 |
| 4% | 7 | T+20 | 18,250 | 12,623 | 69.17% | 68.49% | 2.7667% | 2,368 | 1,159 |

结论：如果允许 lag6/lag7，4% 的历史归档最佳点是 `lag=7,T+20,target=4%`，命中率 69.17%。

### 严格 T+1..T+10 不达到 70%

| target | 最佳 lag | horizon | n | target_hits | target_hit_rate | Wilson lower 95% | mean_target_zero_return |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 3% | 2 | T+10 | 19,649 | 12,654 | 64.40% | 63.73% | 1.9320% |
| 4% | 5 | T+10 | 19,006 | 10,708 | 56.34% | 55.63% | 2.2536% |

结论：历史 70% 左右不是 T+10 内快速兑现结果，主要来自 T+20。

## 稳定性摘要

3% / lag2 / T+20：

- full-period：74.56%，Wilson 下界 73.94%。
- 2021-2023：74.49%。
- 2024-present：74.62%。
- 年度最弱 2023：70.55%，仍接近 70%。

4% / lag5 / T+20：

- full-period：68.48%，Wilson 下界 67.82%。
- 2024-present：70.21%。
- 2023：60.21%，2026：65.47%，年度稳定性弱于 3%。

## 标准回测程序

以后复现本口径，使用现有规范入口：

```bash
PYTHONPATH=src .venv/bin/python scripts/evaluate_mkf_post_cross_lag_target_grid.py \
  --data-root PFrontStockData \
  --config yaml/edge_scout_v1.yaml \
  --start-date 2021-01-01 \
  --workers 8 \
  --output .runtime/mkf-post-cross-lag-target-zero-full.json \
  --summary-csv .runtime/mkf-post-cross-lag-target-zero-full.csv
```

远程验证按项目优先级：WSL 第一，Doris 第二，本地最后。Doris 必须使用 `$HOME/NCN/.venv-doris/bin/python`，不要用系统 `python3`。

## 结论边界

这些结果仅用于离线研究、人工复核和候选质量分析，不是实盘收益承诺、买卖建议、自动下单规则或真实成交证明。`PFrontStockData/` 是调整后研究数据，只能作为离线研究输入，不能作为实盘执行或成交证据。
