# NCN read-only research scanner requirements
# Generated from this project's pyproject.toml plus safe read-only dependencies
# needed for CNstock-style news fetch and read-only web UI experiments.
#
# Deliberately excluded from CNstock-main requirements:
# - torch / torchvision (deep learning runtime not required for NCN scanner setup)
# - scipy / scikit-learn / filterpy (not required by current NCN runtime)
# - trading, broker, portfolio, P&L, or execution-specific dependencies
#
# Supported Python: >=3.12,<3.15

setuptools>=75
wheel>=0.41

# Core NCN package dependencies
baostock>=0.8.9
numpy>=2.0
pandas>=2.2
pyarrow>=18.0
PyYAML>=6.0
tqdm>=4.66

# CNstock-compatible deterministic news/context fetch dependencies
akshare>=1.10.0
requests>=2.31.0

# Read-only Web UI dependencies for safe CNstock web-UI adaptation
flask>=2.3.0
waitress>=2.4.0
werkzeug>=2.3.0

# Optional parsing helpers used by CNstock-style news/web research utilities
lxml>=4.9.0
beautifulsoup4>=4.12.0

# Development/test dependency
pytest>=8.0
